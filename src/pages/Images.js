import Images from '../examples/Images';
import Layout from '../components/Layout';
import React from 'react';

const imge = () => {
	return (
		<Layout>
			<Images />
		</Layout>
	);
};

export default imge;
